import React from "react";

const Email = () => {
  return (
    <div>
      <form>
        <label>Email Login</label>
        <input type="email" />
        <input type="password" />
      </form>
    </div>
  );
};

export default Email;
